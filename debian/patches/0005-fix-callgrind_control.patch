--- valgrind.orig/callgrind/callgrind_control.in
+++ valgrind/callgrind/callgrind_control.in
@@ -29,7 +29,7 @@
   @pids = ();
   open LIST, "vgdb -l|";
   while(<LIST>) {
-      if (/^use --pid=(\d+) for \S*?valgrind\s+(.*?)\s*$/) {
+      if (/^use --pid=(\d+) for \S*?valgrind\.bin\s+(.*?)\s*$/) {
 	  $pid = $1;
 	  $cmd = $2;
 	  if (!($cmd =~ /--tool=callgrind/)) { next; }
